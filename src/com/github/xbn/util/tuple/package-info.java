/**
	<p>For holding moderately-complex objects that will <i>potentially</i> have their {@code toString()}s cocatenated. This properly handles objects whose {@code toString()}s fail, without obscuring any other parts of the {@code toString()} output.</p>

	<p>Example:</p>

	{@code java com.github.xbn.examples.util.PotentialStringConcatWithThreeObjs}
 **/
package  com.github.xbn.util.tuple;
